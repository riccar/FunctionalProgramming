//Setting billAmount and tipPercentage as strings since it's
//better to treat decimal values this way and
//transform them to numbers when needed

const initModel = {
  billAmount: '',
  tipPercent: ''
};

export default initModel;
